services:
  app:
    image: serversideup/php:8.3-fpm-nginx
    ports:
      - '80:8080'
    volumes:
      - '/persist/eddy/shopping:/var/www/html'
    environment:
      - APP_ENV=production
      - APP_DEBUG=false
    restart: unless-stopped
